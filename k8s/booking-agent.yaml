apiVersion: apps/v1
kind: Deployment
metadata:
  name: booking-agent
  namespace: a2a-customer-service
  labels:
    app: booking-agent
    app.kubernetes.io/name: booking-agent
    app.kubernetes.io/component: agent
spec:
  replicas: 2
  selector:
    matchLabels:
      app: booking-agent
  template:
    metadata:
      labels:
        app: booking-agent
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8002"
    spec:
      containers:
        - name: booking-agent
          image: a2a-streaming/booking-agent:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8002
              name: http
          envFrom:
            - configMapRef:
                name: a2a-config
            - secretRef:
                name: aws-credentials
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8002
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: 8002
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: booking-agent
  namespace: a2a-customer-service
  labels:
    app: booking-agent
spec:
  type: ClusterIP
  ports:
    - port: 8002
      targetPort: 8002
      name: http
  selector:
    app: booking-agent

